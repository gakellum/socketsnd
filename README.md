# socketsnd
This sample Go app opens a socket to a receiver and fires off as many 1024-byte messages as it can in 1 second.  Its purpose is to test the data transfer rate via a socket to a receiving process on the same box.
